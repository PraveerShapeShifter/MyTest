// main.js — students will add JavaScript here as features are built

// Render any Lucide icons on the page (<i data-lucide="...">).
// Lucide is loaded via CDN in base.html; guard in case it failed to load.
if (window.lucide) {
    window.lucide.createIcons();
}
